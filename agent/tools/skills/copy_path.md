# copy_path

Purpose: copy a file or directory.

Inputs:
- from: source path.
- to: destination path.
- recursive: copy directories recursively when true.
- overwrite: allow overwrite when true.

Notes:
- Use when you need a backup or duplicate.
