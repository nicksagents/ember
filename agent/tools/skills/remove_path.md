# remove_path

Purpose: delete a file or directory.

Inputs:
- path: target path.
- recursive: remove directories recursively when true.
- force: ignore missing paths when true.

Notes:
- Use with care; deletions are irreversible.
