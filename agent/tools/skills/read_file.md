# read_file

Purpose: read text from a file.

Inputs:
- path: file to read.
- start: optional byte offset.
- length: optional byte length to read.

Notes:
- Use `start`/`length` for large files.
- Returns UTF-8 text.
